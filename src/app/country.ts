export interface Country {

    name:string;
   capital:string;
   region:string;
   subregion:string;
   callingCodes:string;
   language:[];
}

// interface used to fetch the specific property from API 
